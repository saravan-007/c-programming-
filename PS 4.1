#include <stdio.h>

int main() {
    
    int  i,n,rev=0,rem;
    printf("enter n :");
    scanf("%d",&n);
    for (i=n; n>0; i++){
        rem = n%10;
        rev = rev*10+rem;
        n=n/10;
        
    }
    {printf("the reverse number is:%d",rev);}

    return 0;
}
output
user input 2345
the reverse number is 5432
